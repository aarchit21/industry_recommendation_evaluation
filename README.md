# LLM Evaluation Toolkit (Ollama Setup)

This pipeline extracts fixes from customer-support remediation text and utilizes a local instance of Ollama to analyze dimensions of `Relevance`, `Actionability`, `Concreteness`, and `Feasibility`.

## Dependencies
1. Install requirements using Python (3.9+ recommended):
   ```bash
   pip install -r requirements.txt